# Contributing

## Development

Use Python 3.12+, install the project and development dependencies, then run:

```bash
ruff check .
mypy src
pytest
```

## Pull requests

Keep changes focused. Add tests for behavior changes and update schemas or documentation when contracts change. Do not merge with failing CI.
